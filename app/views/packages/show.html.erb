<div>
  Name: <%= @package.name %>
</div>

<div>
  Versions: <% @package.versions.each do |version| %>
    <span class="<%= 'current_version' if @package.current_version == version %>">
      <%= version %>
    </span>
  <% end %>
</div>

<div>
  Authors: <%= @package.authors.map(&:name).join(', ') %>
</div>

<div>
  Description: <%= @package.description %>
</div>

